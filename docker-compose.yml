services:
  potato_bot:
    build: .                  
    container_name: potato_bot 
    restart: unless-stopped 
    env_file:
      - .env                
    volumes:
      - ./bot_database.db:/app/bot_database.db